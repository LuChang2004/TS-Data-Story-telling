# 项目说明文档：Taylor Swift With Her Music
对 Taylor Swift 音乐生涯中的12张主要专辑进行的数据可视化网页项目

---

## 一、数据故事

**故事核心**：以 **十二张录音室专辑** 为骨架，把 Taylor Swift 的职业生涯拆成「可读的叙事 + 可量的数据」——左侧是专辑本体（封面、曲目），中间是文字与图像构成的 **时代叙事**，右侧是 **音乐分析**（和弦、编曲占比）与 **成就分析**（销量估计、格莱美提名与获奖）。整站通过纵向滚动模拟 **时间推进**：从落地页进入时间轴，再逐张专辑向下阅读。

**选择理由**：

- Taylor Swift 作为我的音乐启蒙，构建了我对于当代乡村音乐以及流行音乐的审美观
- Taylor Swift 的音乐生涯呈现出几个明显的创作时期，风格转变巨大。创作理念、现实境遇的转变促使她不断转型，最终成为全球乐坛超一线明星，其音乐数据具有巨大的研究价值与发掘潜力
- Taylor Swift的乐迷数量庞大、范围极广，使得该项目具有天然的传播性与情感共鸣。

---

## 二、数据：来源、结构、收集与处理

### 数据来源（原始获取方式）

本项目表格中的**文字型事实与分类信息**，主要由作者使用 **Kimi** 等工具辅助检索、整理与汇总后写入 CSV；**并非**来自单一官方开放数据集。其中：

- **和弦进行（Chord Progression）、调式/调性（Mode / Key）**：**一部分**由 AI 辅助调研、归纳后填入；**另一部分**由作者收听 **YouTube** 等公开音视频后**对照、核对并手动录入**。二者在整张表中混用，未在单元格层面逐条标注来源。
- **乐器占比等结构化字段**：同样经 Kimi 辅助整理后进入 `src/data/ts-data.csv`，可视化前在代码侧做了格式统一与名称清洗。

**说明**：AI 生成或二手整理的音乐理论信息可能存在误差；YouTube 版本（现场、改编）也可能与录音室版不一致。本项目的图表展示的是**当前表格中的数据**，读者宜将其理解为「用于叙事与可视化的研究用数据集」，而非权威乐谱或官方标注。

### 工程中的文件对应关系

| 资源 | 路径 / 说明 |
| --- | --- |
| 专辑与曲目级音乐数据 | `src/data/ts-data.csv`（上述方式整理后落盘的主表） |
| 专辑级成就与背景 | `src/data/ts-data-02.csv`（销量、RIAA、格莱美、主单曲、制作人等，经 Kimi 等辅助整理） |
| 叙事文案与配图 | `public/data/experience.json`（按专辑编号 `1`…`12` 编写段落与可选图片） |
| 页面视觉主题 | `public/data/album-themes.json`（每专渐变、`darkMode`、可选 `accent` / `accent2`） |
| 落地页封面散落布局（可选） | `public/data/landing-cover-layout.json` |
| 封面与试听（可选） | `public/Album Covers/{n}.JPG`、`public/TS Music/{n}.mp3` |

### 数据结构（概要）

`**src/data/ts-data.csv`**（一行一首歌）

- 专辑维度：`Album Number`、`Album Name`、`Release Date`、`Taylor's Musical Era`、专辑级乐器综述等。
- 曲目维度：`Songs Included in Album`、`Song Chord Progressions`、`Song Modes/Keys`、`Song Instrumentation Types & Proportions`（含百分比文本，如 `AcousticGuitar(35%)`）。

`**src/data/ts-data-02.csv`**（一行一张专辑）

- 销量估计、RIAA、`Grammy_Wins` / `Grammy_Nominations`、`Key_Awards_Summary` 等，经解析后用于右侧 **Achievement** 模式下的折线与堆叠条形图。

**运行时合并**

- 构建阶段用 **Papa Parse** 解析主 CSV，按专辑编号归并为 `AlbumBundle`（含 `songs[]`、`chordCounts`、`instrumentWeights` 等聚合字段）。
- 启动时 **fetch** JSON，将 `experience.json` 挂到对应 `albumNumber` 的专辑上。

### 收集与处理过程

1. **人工 + 工具整理**：使用 Kimi 检索、归纳专辑/曲目信息，和弦与调性在 AI 辅助与 YouTube 人工听辨之间分工录入，最终统一写入 CSV。
2. **规范化表头**：`tsDataCsv.ts` 将列名 slug 化，兼容 BOM 与轻微命名差异。
3. **乐器字符串解析**：`instrumentNames.ts` 等将「乐器名 + 百分比」拆成结构化列表，并做名称清洗以便图标与汇总一致。
4. **聚合**：在 `aggregate` 相关逻辑中从曲目汇总和弦频次、乐器权重，供和弦可视化与乐器排行条使用。
5. **成就表**：`tsAchievementCsv.ts` 解析 `src/data/ts-data-02.csv`，映射为按专辑编号索引的结构，供 `vizAchievementCharts.ts` 绘制。

数据更新时：优先改 CSV / JSON，再 `npm run build` 或本地 `npm run dev` 验证解析与图表是否正常。

---

## 三、叙事与交互设计

### 叙事逻辑（宏观）

1. **落地页**：全屏标题 + 十二张封面散落，**滚动进度**驱动封面向外扩散，暗示「从一张脸孔到整条生涯」。
2. **时间轴导航**：按 **Era** 分组（连续相同 era 标签合并为一组），点击跳转对应 `section`，与滚动位置联动高亮。
3. **每一时代区块**：三栏——**专辑与曲目** | **Taylor's journey（叙事）** | **Analysis（数据）**，把「听什么 / 故事说什么 / 数据证明什么」放在同一屏内读完。
4. **滚动即时间**：背景渐变与 UI 色（含可选每专高亮）随滚动在相邻专辑主题间 **插值**，强化「时间在流动」。

### 交互项与设计意图


| 交互                                    | 意图说明                                                                                                                   |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **落地页向下滚动**                           | 完成从「总览意象」到「可导航时间线」的过渡；滚动量同时驱动 **主题从默认 landing 色过渡到当前专辑色**（`scrollTheme` + `albumThemes`）。                              |
| **点击时间轴上的专辑名**                        | **揭示空间维度中的时间锚点**：不必纯靠滚找位置，支持跳跃阅读；`aria-current` 标明当前时代，兼顾键盘与读屏。                                                        |
| **滚轮经过叙事栏（桌面 sticky 布局）**             | **优先读完长文再翻页**：在网格贴顶、指针不在专辑列/时间轴/分析栏时，滚轮先滚动「Taylor's journey」内部，避免长段落被一页滚没；意图是 **控制信息释放节奏**。                            |
| **点击曲目名**                             | **从列表进入单曲维度**：弹出层展示和弦进行、调式/调性、乐器占比条与图标，让观者看到 **同一专辑内曲目之间的编曲差异**。                                                       |
| **首次点击/触摸页面（音频解锁提示）**                 | 满足浏览器 **自动播放策略**：用户手势后才开始专辑 BGM 的淡入切换，减少无声困惑与策略拦截。                                                                     |
| **滚动经过不同专辑（IntersectionObserver）**    | **声音随可见时代切换**：当前屏主导的专辑对应 `public/TS Music/{n}.mp3` 交叉淡入，用听觉强化「你现在在哪一段生涯」。                                              |
| **Analysis → Musical 标签**             | **锁定音乐本体**：和弦分布 + 乐器排行 **随滚动在相邻两张专辑间混合**（双图层透明度 + 排行卡片位置/高度 morph），意图是让观者 **感知相邻 era 的编曲结构如何渐变**。                      |
| **桌面下当前 era 的 grid 贴顶时**              | **稳定阅读 Musical 视图**：滚动混合退化为「仅当前专辑」，避免 sticky 状态下图层与滚动争夺注意力。                                                            |
| **Analysis → Achievement 标签**         | **切换到商业与行业认可维度**；销量折线与格莱美堆叠条仅包含 **从第一张到当前滚动所处 era 为止** 的专辑——**向上滚会减少右侧点数**，意图是强调 **「故事读到哪，成就就展示到哪」** 的累积叙事，而非一次性甩出全生涯。 |
| **时代区块内视差（`eraParallax`）**            | 离开屏与进入屏的列有轻微 **滞后/领先**，增强纵向滚动的 **深度感**；在接近 sticky 钉扎位置时减弱，避免与 `position: sticky` 抖动冲突。                                 |
| **可选：`album-themes.json` 中 `accent`** | 每张专辑可单独指定高亮色，使时间轴、链接、图表强调色与 **该 era 视觉气质** 一致。                                                                         |


---

## 四、技术实现

- **构建与语言**：TypeScript，**Vite** 打包；主入口 `src/main.ts`，样式 `src/styles.css`。
- **CSV**：**Papa Parse** 解析；主表以 `?raw` 形式导入，成就表同理。
- **DOM 与可视化**：原生 HTML/CSS/TS，无 React/Vue；图表为 **SVG**（成就模块由 `vizAchievementCharts.ts` 生成）。
- **关键模块**：`scrollTheme.ts`（滚动锚点、主题混合、sticky 检测）、`vizDock.ts`（第三栏 Tab 与 rAF 刷新）、`albumThemes.ts`（CSS 变量写入）、`landingHero.ts`（落地滚动进度）、`narrativeFirstWheel.ts`（叙事栏滚轮优先）、`albumAudio.ts`（音频交叉淡化）。
- **辅助脚本**（`package.json`）：乐器名提取、翻译管线等（`scripts/`）。

部署：仓库含 `vercel.json`，可配合 Vite `build` 输出静态站点。

---

## 五、反思（请个人化修改本节）

以下为与实现难度相关的 **参考角度**，提交作业时请改成自己的真实体会。

**较满意的部分（示例）**

- 三栏同时服务 **叙事、列表、分析**，且滚动与主题、音频联动，形成较完整的「沉浸式年表」体验。
- Musical 模式下 **跨专辑插值** 与乐器卡 **morph**，能把「渐变」做成可感知动效，而不只是换一张静态图。
- Achievement 模式 **随滚动截断时间范围** 的设计，与「读到哪里看到哪里」的叙事一致。

**较具挑战的部分（示例）**

- **Sticky 布局 + 滚轮路由 + 视差** 同时存在时，需反复调试阈值（时间轴高度、`--timeline-h`、钉扎检测容差），避免抖动或抢滚动。
- **CSV 字段不规则**（如 `Unknown`、缺失和弦、乐器字符串格式不一）需要在解析层宽容处理，并保证空状态 UI 可理解。
- **音频与浏览器策略**：首次交互解锁、切换专辑时的音量与淡入淡出需要与滚动检测配合，避免卡顿或重复触发。

---

## 附录：与说明文档对应的主要文件


| 内容            | 文件                                                      |
| ------------- | ------------------------------------------------------- |
| 应用组装、时间轴、曲目弹层 | `src/main.ts`                                           |
| 主数据解析         | `src/tsDataCsv.ts`、`src/aggregate.ts`                   |
| 成就数据解析与图表     | `src/tsAchievementCsv.ts`、`src/vizAchievementCharts.ts` |
| 第三栏逻辑         | `src/vizDock.ts`                                        |
| 滚动与主题         | `src/scrollTheme.ts`、`src/albumThemes.ts`               |
| 落地页           | `src/landingHero.ts`                                    |
| 叙事栏滚轮         | `src/narrativeFirstWheel.ts`                            |
| 视差            | `src/eraParallax.ts`                                    |


---

*文档版本与仓库实现同步维护；若交互有迭代，请同步更新第三节表格。*